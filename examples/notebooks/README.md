# Notebook Gallery (Draft)

- `pilot_quickstart.ipynb`: high-level example for ingesting a CSV, training `OrchidRecommender`, and exporting metrics/audit logs.
- Contributors should add real-world notebooks here (remove confidential data). Run `pip install orchid-ranker[observability,connectors]` to access optional helpers.

To execute:

```bash
jupyter notebook examples/notebooks/pilot_quickstart.ipynb
```
